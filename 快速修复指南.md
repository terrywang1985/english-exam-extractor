# 快速修复指南

## 问题1：下划线丢失 ✅ 已修复

### 问题表现
提取后的HTML中，填空题的下划线（答题位置）消失了。

### 根本原因
原页面使用 `<bdo class="mathjye-underline">` 标签，提取时被当作普通文本处理。

### 解决方案
使用 `html-extractor-v2.js`，已添加：
- 识别下划线标签
- 替换为带样式的 `<span class="answer-blank">________</span>`
- CSS样式显示为明显的下划线

---

## 问题2：完形填空不完整 ✅ 已修复

### 问题表现
完形填空只显示第一句话，后面的内容全部丢失。

### 示例对比

**修复前**（只有第一段）：
```
（15分） When people wanted to find their way to a place in the past...
```

**修复后**（完整内容）：
```
（15分） When people wanted to find their way to a place in the past，
they used to have to buy a map.They don't have to do that anymore.
Now they can either buy a GPS or go online and find maps for just about
（16）________.

Is this an （17）________？Perhaps，but some people think some of the 
online companies are going too far because they have been sending out 
cars with photographic equipment on their roofs to photograph every 
street and house in the country.

One of these cars arrives on a Wednesday morning in the quiet English 
village of Broughton.The camera was placed on a meter—high pole on top 
of the car and could see over walls and into people's gardens.Some of 
the villagers came and stood around the car，asking the drivers and 
photographer to （18）________.Journalists quickly arrived on the scene，
and soon the event was news all over the country.

... (完整的所有段落)
```

### 根本原因
完形填空的HTML结构特殊：
- 题号（16）（17）嵌入在段落中
- 每个题号后有答案标记 `<div class="sanwser">D</div>`
- 原提取器移除答案时，把后续内容也删除了

### 解决方案
1. 检测题型（通过 `h3.ques-type`）
2. 完形填空特殊处理：
   - 保留HTML结构
   - 只移除 `.quizPutTag`
   - 将答案替换为空白而不是删除
   - 保留所有段落和嵌入的题号

---

## 立即使用修复版本

### 步骤1：重新加载插件
1. 打开 `chrome://extensions/`
2. 找到"英语试卷提取器"
3. 点击刷新按钮 🔄

### 步骤2：提取试卷
1. 打开试卷页面（或刷新页面）
2. 点击浏览器工具栏的插件图标
3. 点击"导出为HTML"按钮
4. 等待下载完成

### 步骤3：验证修复效果
打开下载的HTML文件，检查：
- ✅ 填空题有明显的下划线：________
- ✅ 完形填空内容完整，所有段落都存在
- ✅ 题号（16）（17）等清晰标记
- ✅ 排版清晰，易于阅读和打印

---

## 测试清单

使用提取的HTML文件进行以下测试：

### 基础测试
- [ ] 打开HTML文件能正常显示
- [ ] 中文无乱码
- [ ] 所有题目都完整显示

### 下划线测试
- [ ] 单选题中的下划线可见
- [ ] 填空题的答题位置清晰
- [ ] 下划线长度合适（约100px）

### 完形填空测试  
- [ ] 能看到完整的文章内容
- [ ] 所有段落都存在（不只是第一段）
- [ ] 嵌入的题号（16）（17）等清晰可见
- [ ] 空白处用下划线标记

### 打印测试
- [ ] 按Ctrl+P能正常打印
- [ ] 打印预览格式正确
- [ ] 所有内容在打印页面可见

---

## 故障排除

### 如果下划线还是没有显示
1. 检查HTML文件源代码，搜索 `answer-blank`
2. 如果找不到，说明提取器没有正确运行
3. 重新加载插件并重试

### 如果完形填空还是不完整
1. 在浏览器中打开原试卷页面
2. 按F12打开开发者工具
3. 在Console控制台执行：
```javascript
document.querySelectorAll('h3.ques-type').forEach(h3 => {
  console.log(h3.textContent);
});
```
4. 检查是否能找到"完形填空"题型

### 如果插件无法使用
1. 确认Chrome版本 ≥ 88
2. 确认已启用开发者模式
3. 尝试卸载后重新加载插件

---

## 技术支持

如果问题仍然存在：
1. 查看浏览器控制台（F12）的错误信息
2. 检查 `html-extractor-v2.js` 文件是否存在
3. 确认 `manifest.json` 引用了正确的文件

---

**快速链接：**
- 详细修复说明：`问题修复说明.md`
- 使用手册：`使用说明-v2.txt`
- 测试脚本：`test-console.js`